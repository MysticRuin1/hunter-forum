services:
  - type: web
    name: hunternet
    env: node
    plan: free
    buildCommand: npm run render-build
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
